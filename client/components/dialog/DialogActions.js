import { DialogActions as DialogActionsMui } from '@material-ui/core';
import styled from 'styled-components';

const DialogActions = styled(DialogActionsMui)`
  && {
    margin: 10px 20px 24px 24px;
  }
`;

export default DialogActions;
