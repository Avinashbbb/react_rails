import { RadioGroup } from '@material-ui/core';
import styled from 'styled-components';

const RadioGroupRowWrapper = styled(RadioGroup)`
   flex-direction: row !important;
`;

export default RadioGroupRowWrapper;
